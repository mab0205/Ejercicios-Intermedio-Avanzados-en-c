#include<stdio.h>

/*Escreva uma funcao recursiva imprimeDigitosInv(int N), que recebe um
inteiro positivo N e imprime os dıgitos de N, um por linha, 
do menos significativo ao mais significativo. Como exemplo, a chamada imprimeDigitosInv(123)*/

/*void imprimeDigitosInv(int n){
if (n < 10) {
        printf("%d\n", n);
        return ;
    }
    printf("%d\n", n % 10);
    imprimeDigitosInv(n / 10);
}*/

int main(int argc, char const *argv[])
{
   //imprimeDigitosInv(234);
   

    return 0;
}
